# PIME Database - Environment Variables Example
# Copia este archivo y renómbralo según el ambiente:
# - .env.dev (desarrollo local)
# - .env.test (testing)
# - .env.prod (producción - NUNCA subir a Git)

# PostgreSQL Connection
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=pime_dev
POSTGRES_USER=pime_user
POSTGRES_PASSWORD=change_me_in_production

# pgAdmin (solo para desarrollo)
PGADMIN_EMAIL=admin@pime.local
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# Notas de seguridad:
# - En producción, usa contraseñas fuertes y únicas
# - Nunca subas archivos .env al repositorio
# - Usa GitHub Secrets para CI/CD
# - Rotá las contraseñas regularmente
